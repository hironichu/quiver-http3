# EncodingAndDecoding

How to encode and decode HTTP field sections with `QPACK`.

## Overview

Use `QPACKEncoder` to build wire-format blocks and `QPACKDecoder` to reconstruct field lists. Primitive helpers (`QPACKInteger`, `QPACKString`) support low-level workflows.

## See Also

- <doc:QPACK>
- <doc:ErrorHandling>
